# 症智明辨 — 快速启动指南

## 第一步：安装 Python（如果还没装）
- 访问 https://www.python.org/downloads/ 下载 Python 3.10 或更高版本
- 安装时**务必勾选** "Add Python to PATH"

## 第二步：安装依赖
打开命令行（Win+R → 输入 cmd → 回车），进入本目录后运行：
```
pip install -r requirements.txt
```

## 第三步：启动系统
双击 **「启动应用.bat」** 或命令行运行：
```
streamlit run app.py --server.port 8503
```
浏览器会自动打开 http://localhost:8503

## 可选：接入大模型（DeepSeek / 通义千问等）
1. 把 `.streamlit/secrets.toml.example` 复制一份，改名为 `secrets.toml`
2. 打开后填入你自己的 API Key：
```toml
[llm]
provider  = "deepseek"
api_key  = "sk-你的API密钥"
base_url = "https://api.deepseek.com/v1"
model    = "deepseek-chat"
temperature = 0.3
```
3. 重启应用即可启用 AI 智能解读功能

> 不配置也能正常使用！系统会自动走规则化降级路径，机器学习辨证 + SHAP 热力图全部正常。

---

⚠️ **免责声明**：本系统为中医辅助辨证科研教学工具，所提供的证型分析、概率分布、SHAP 可解释性分析、方剂建议与调摄指导均**仅供科研教学参考**，不构成医疗处方、诊断意见或治疗方案。证型预测由机器学习模型输出，大模型仅做科普解读，不能替代执业中医师的四诊合参与辨证论治。
